// object Test {

//  		def minTs(lst: List[Int]) :Int = {
//  			lst.reduce((x,y) => if(x>y) x else y)
//  		}
//       def main(args: Array[String]) {
//       	val a = List(1,4,5555,6,7,1231,12,-24,3125, 10, -9)
//       	println(a.map(x => {
//       		if(x < minTs(a)) x
//       		else -1
//       		}))
//       	// println(a.reduce((x,y) => if(x>y) x else y))
//       	// println(minTs(a))
//      }
// }


